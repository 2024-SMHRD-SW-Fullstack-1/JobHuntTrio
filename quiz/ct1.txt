주어진 문자열에서 가장 많이 반복되는 문자를 찾아 그 개수를 반환하는 함수를 작성하시오.
max(Counter(string).values())
len(set(string))
sum(Counter(string).values())
max(Counter(string).keys())
1
주어진 이진 트리에서 가장 깊은 깊이를 구하는 함수를 작성하시오.
max_depth(tree)
tree.height()
depth(tree)
tree.max_depth()
4